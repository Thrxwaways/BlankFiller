<Window x:Class="BlankFiller.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CACTUS"
        Height="700" Width="500"
        MinHeight="650" MinWidth="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <LinearGradientBrush x:Key="NeonBorder" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF00BFA5" Offset="0"/>
            <GradientStop Color="#FF6A1B9A" Offset="0.5"/>
            <GradientStop Color="#FF0097A7" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="GlassBg" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0A0A1A" Offset="0"/>
            <GradientStop Color="#0D0D2B" Offset="0.5"/>
            <GradientStop Color="#0A0A1A" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="CactusGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FF26A69A" Offset="0"/>
            <GradientStop Color="#FF7E57C2" Offset="0.5"/>
            <GradientStop Color="#FF26C6DA" Offset="1"/>
        </LinearGradientBrush>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border Background="#151530" BorderBrush="#FF26A69A" BorderThickness="1" CornerRadius="10" x:Name="Border">
                <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#FF26A69A"
                      HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#FF40C4B0"/>
                    <Setter TargetName="Arrow" Property="Fill" Value="#FF40C4B0"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="NeonComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#151530"/>
            <Setter Property="Foreground" Value="#E0E0FF"/>
            <Setter Property="BorderBrush" Value="#FF26A69A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="IsEditable" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              ContentStringFormat="{TemplateBinding DisplayMemberPath}"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"
                                              Margin="14,0,0,0" Visibility="Visible"/>
                            <TextBlock x:Name="Placeholder" Text="{TemplateBinding Tag}"
                                       Foreground="#555577" VerticalAlignment="Center"
                                       Margin="14,0,0,0" Visibility="Collapsed" IsHitTestVisible="False"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Fade" StaysOpen="False">
                                <Border x:Name="DropDownBorder" Background="#1A1A35"
                                        BorderBrush="#FF26A69A" BorderThickness="1" CornerRadius="8"
                                        Margin="0,4" MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer><ItemsPresenter/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="SelectedItem" Value="{x:Null}">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#1A1A35"/>
                        <Setter Property="Foreground" Value="#E0E0FF"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" x:Name="Border">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#2A2A55"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#151530"/>
            <Setter Property="Foreground" Value="#E0E0FF"/>
            <Setter Property="BorderBrush" Value="#FF6A1B9A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="CaretBrush" Value="#E0E0FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#151530"/>
            <Setter Property="Foreground" Value="#E0E0FF"/>
            <Setter Property="BorderBrush" Value="#FF6A1B9A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="CaretBrush" Value="#E0E0FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonButton" TargetType="Button">
            <Setter Property="Background" Value="#151530"/>
            <Setter Property="Foreground" Value="#FF26A69A"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#FF26A69A" BorderThickness="1.5" CornerRadius="12" x:Name="border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E1E45"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF40C4B0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D0D25"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:1"/>
        </Storyboard>
        <Storyboard x:Key="ErrorShake">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="0" To="8" Duration="0:0:0.05" AutoReverse="True" RepeatBehavior="3x"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="20" Background="{StaticResource GlassBg}"
            BorderBrush="{StaticResource NeonBorder}" BorderThickness="2" Margin="10">

        <Grid Margin="30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="✕" HorizontalAlignment="Right"
                    Background="Transparent" Foreground="#444466" BorderThickness="0"
                    FontSize="18" Cursor="Hand" Click="CloseButton_Click" Margin="0,-10,-10,0"/>

            <TextBlock Grid.Row="1" Text="C A C T U S" FontSize="42" FontWeight="ExtraBold"
                       FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="0,10,0,0"
                       Foreground="{StaticResource CactusGradient}"/>

            <TextBlock Grid.Row="2" Text="beta 0.1" FontSize="13" FontFamily="Segoe UI"
                       Foreground="#3A3A5A" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <ComboBox Grid.Row="3" x:Name="InspectorCombo"
                      Style="{StaticResource NeonComboBox}" Tag="Инспектор"
                      Margin="0,5" Height="44" SelectedIndex="-1"
                      DisplayMemberPath="FullName"
                      SelectionChanged="InspectorCombo_SelectionChanged"/>

            <ComboBox Grid.Row="4" x:Name="MedicCombo"
                      Style="{StaticResource NeonComboBox}" Tag="Фельдшер"
                      Margin="0,5" Height="44" SelectedIndex="-1"
                      SelectionChanged="MedicCombo_SelectionChanged"/>

            <Border Grid.Row="5" Margin="0,5" Height="44">
                <Grid>
                    <TextBox x:Name="PasswordTextBox" Style="{StaticResource NeonTextBox}"
                             Visibility="Collapsed" Padding="12,10,44,10"/>
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource NeonPasswordBox}"
                                 Padding="12,10,44,10" PasswordChanged="PasswordBox_PasswordChanged"/>
                    <TextBlock x:Name="PasswordPlaceholder" Text="Введите пароль"
                               Foreground="#555577" VerticalAlignment="Center"
                               Margin="16,0,0,0" IsHitTestVisible="False"/>
                    <Button x:Name="TogglePasswordButton" Content="👁"
                            Background="Transparent" Foreground="#555577"
                            BorderThickness="0" FontSize="16" Cursor="Hand"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Margin="0,0,8,0" Click="TogglePasswordButton_Click"/>
                </Grid>
            </Border>

            <Border x:Name="WarningBorder" Grid.Row="7" Background="#1A1808"
                    BorderBrush="#8B7A2F" BorderThickness="1"
                    CornerRadius="10" Padding="12,8" Margin="0,3"
                    Opacity="0" Visibility="Collapsed">
                <Border.RenderTransform><TranslateTransform/></Border.RenderTransform>
                <Grid>
                    <TextBlock x:Name="WarningText" Text=""
                               Foreground="#CCAA44" FontSize="12" FontWeight="SemiBold"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Button Content="✕" HorizontalAlignment="Right"
                            Background="Transparent" Foreground="#CCAA44"
                            BorderThickness="0" FontSize="11" Cursor="Hand"
                            Click="HideWarning_Click" Padding="4,0"/>
                </Grid>
            </Border>

            <Border x:Name="ErrorBorder" Grid.Row="8" Background="#1A0A0F"
                    BorderBrush="#FF6B2F3A" BorderThickness="1"
                    CornerRadius="10" Padding="12,8" Margin="0,3"
                    Opacity="0" Visibility="Collapsed">
                <Border.RenderTransform><TranslateTransform/></Border.RenderTransform>
                <Grid>
                    <TextBlock x:Name="ErrorText" Text=""
                               Foreground="#FF8B5A" FontSize="12" FontWeight="SemiBold"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Button Content="✕" HorizontalAlignment="Right"
                            Background="Transparent" Foreground="#8B5A6A"
                            BorderThickness="0" FontSize="11" Cursor="Hand"
                            Click="HideError_Click" Padding="4,0"/>
                </Grid>
            </Border>

            <Button Grid.Row="10" Content="Авторизоваться в систему"
                    Style="{StaticResource NeonButton}" Margin="0,20,0,10"
                    VerticalAlignment="Bottom" Click="LoginButton_Click"/>
        </Grid>
    </Border>
</Window>